spring.application.name=yvonne-portfolio
server.port=${PORT:8080}
spring.thymeleaf.cache=false

portfolio.contact.recipient=mukabera255@gmail.com
portfolio.mail.enabled=${PORTFOLIO_MAIL_ENABLED:false}

spring.mail.host=${PORTFOLIO_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${PORTFOLIO_MAIL_PORT:587}
spring.mail.username=${PORTFOLIO_MAIL_USERNAME:}
spring.mail.password=${PORTFOLIO_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
